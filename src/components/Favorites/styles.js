import styled from "styled-components";

export const MainTitle = styled.h2`
    color: ${({ theme }) => theme.text};
    text-align: center;
`

export const MovieCounter = styled.p`
    color: ${({ theme }) => theme.text};
    text-align: center;
`